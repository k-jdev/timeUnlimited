export default function WatchDetailPage({
  params,
}: {
  params: Promise<{ id: string }>
}) {
  return (
    <div className="min-h-screen px-4 pt-20 lg:px-10">
      {/* TODO: Watch detail page — images, specs, description */}
    </div>
  )
}
